image: node

stages:
  - lint
  - prettier

eslint:
  tags:
    - docker
  stage: lint
  script:
    - npm run lint

prettier:
  tags:
    - docker
  stage: prettier
  script:
    - npm run prettier
